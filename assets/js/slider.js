var slideCount = document.querySelectorAll().length;
var slideWidth = document.querySelectorAll().width();
var slideHeight = docuemnt.querySelectorAll().height();
var sliderU1Width = slideCount * slideWidth;

const slider = document.querySelector("#slider");
slider.style.width = slideWidth;
slider.style.height = slideHeight;

function moveleft() {}
